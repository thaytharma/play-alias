import React from "react";
import styles from "./Footer.module.scss";

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>Copyright @ Thayanan Tharmapalan</footer>
  );
};

export default Footer;
